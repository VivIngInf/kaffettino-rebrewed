{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "postcss ./css/styles.css -o ./css/output.css --env production && mkdir -p ../data/css && mkdir -p ../data/js && cp ./index.html ../data/ && cp ./css/output.css ../data/css/ && cp -r ./js/* ../data/js/",
    "serve": "npm run build && npm-run-all --parallel watch:styles serve:dev",
    "watch:styles": "nodemon --watch ./css/styles.css --watch ./index.html --exec \"npm run build\"",
    "serve:dev": "browser-sync start --server --files './css/styles.css, ./index.html'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.17",
    "autoprefixer": "^10.4.22",
    "browser-sync": "^3.0.4",
    "cssnano": "^7.1.2",
    "nodemon": "^3.1.11",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.1",
    "tailwindcss": "^4.1.17"
  }
}
